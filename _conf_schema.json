{
    "secret_id": {
        "type": "string",
        "description": "腾讯云SecretId",
        "hint": "从控制台 > 访问管理 > API密钥管理获取"
    },
    "secret_key": {
        "type": "string",
        "description": "腾讯云SecretKey",
        "input_type": "password"
    },
    "region": {
        "type": "string", 
        "description": "API区域",
        "default": "ap-singapore",
        "hint": "参考：ap-beijing(北京)、ap-singapore(新加坡)"
    },
    "zone_id": {
        "type": "string",
        "description": "站点ID(ZONE ID)",
        "hint": "从控制台 > CDN > 域名管理 > 域名详情页获取"
    }
}
